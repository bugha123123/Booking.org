@using Hotel.org.Interface
@inject IAdminService _adminservice

<section class="mt-5">
    <div class="flex items-center justify-between">
        <h2 class="text-xl font-bold">User Reviews</h2>
        
    </div>
    <div class="mt-4 grid gap-4">
        @foreach (var review in await _adminservice.GetAllReviewsForEveryUser())
        {
            <div class="flex items-start gap-4">
                <span class="relative flex shrink-0 overflow-hidden rounded-full h-8 w-8">

                    <img class="aspect-square h-full w-full" src="@Url.Content("~/ProfileImage/" + review.user.ProfileImageFileName)" />


                </span>
                <div class="flex-1">
                    <div class="flex items-center justify-between">
                        <div class="font-medium">@review.AddedBy</div>
                        <div class="flex items-center gap-1 text-xs font-semibold">
                            @foreach (var star in Enumerable.Range(1, 5))
                            {
                                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-4 w-4">
                                    <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
                                </svg>
                            }
                        </div>
                    </div>
                    <p class="mt-2 text-sm text-gray-500 dark:text-gray-400">@review.Comment</p>
                </div>
            </div>
        }
    </div>
</section>